{
  "name": "cleanboost",
  "version": "3.1.2",
  "description": "DEPRECATED placeholder. The canonical cleanboost package lives at npm-cli/ (also published as cleanboost). This stub package is no longer needed and only exists for backward-compatibility with installs that resolved it as a transient alias.",
  "keywords": ["cleanboost", "optimizer", "deprecated"],
  "license": "MIT",
  "engines": {"node": ">=18"},
  "scripts": {
    "postinstall": "node scripts/postinstall.js"
  },
  "bin": {
    "cleanboost":      "./bin/cleanboost.js",
    "cleanboost-cli":  "./bin/cleanboost.js",
    "cb":              "./bin/cleanboost.js"
  },
  "files": [
    "bin/cleanboost.js",
    "scripts/postinstall.js",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {}
}
